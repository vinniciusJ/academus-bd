package com.unioeste.academus.repositories;

import com.unioeste.academus.models.SubjectClassEnrollment;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SubjectClassEnrollmentRepository extends JpaRepository<SubjectClassEnrollment, Long> {
}
